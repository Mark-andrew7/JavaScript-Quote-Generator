This is a Quote generator project which generates a new quote upon pressing a button
Technologies used are html for the markup, CSS for the styling and Javascript to make the quotes generate randomly
